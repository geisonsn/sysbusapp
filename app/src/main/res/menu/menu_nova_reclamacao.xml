<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <item
        android:id="@+id/action_save_reclamacao"
        android:title=""
        android:icon="@drawable/ic_action_save"
        android:orderInCategory="100"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_recarregar"
        android:title="Recarregar dados"
        android:icon="@drawable/ic_action_cached"
        android:orderInCategory="100"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_progresso"
        android:title="Aguarde..."
        android:visible="false"
        android:actionLayout="@layout/progress_bar"
        app:showAsAction="ifRoom" />

</menu>
